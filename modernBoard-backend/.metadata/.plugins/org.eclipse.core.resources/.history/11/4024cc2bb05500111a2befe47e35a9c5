package com.example.board.mapper;

import java.util.List;
import java.util.Map;

import org.apache.ibatis.annotations.Mapper;

import com.example.board.dto.BoardAuthorDTO;
import com.example.board.vo.BoardVO;

@Mapper
public interface BoardMapper {
	List<BoardAuthorDTO> getBoardList(Map<String, Object> params);
	
	int getBoardTotalCount(Map<String, Object> params);
	
	BoardVO getByIdBoardVo(Long id);
	
	void insertBoardVo(BoardVO vo);
	
	void updateBoardVo(BoardVO vo);
	
	void deleteBoardVo(Long id);
}
